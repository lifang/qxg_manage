<div class="question_content" onclick="toggleNext(this)">
  <%= question.content %>
</div>

<div class="question_options" style="display: none">
  <ul>
    <% @branch_question_hash[question.id][0].options.split(";||;").each  do |option| %>
      <li><input name="answer[]" type="checkbox"/>
        <%= option %>
      </li>
    <% end %>
  </ul>

</div>
