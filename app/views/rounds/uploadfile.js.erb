<% @notice_info = @hash_result[:notice] %>
<% @round = @hash_result[:round]%>
$("#error_bar").html('<%= j (render :partial => 'error_infos') %>');
$("#upload_bar").html('<%= j (render :partial => 'uploadzip') %>');
$("#rounds_list").html("<%= j (render  "rounds/rounds_list") %>");
//Location.reload();
